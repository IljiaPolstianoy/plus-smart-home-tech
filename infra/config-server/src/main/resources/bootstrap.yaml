spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations:
            - "classpath:/config/telemetry/{application}"
            - "classpath:/config/commerce/{application}"