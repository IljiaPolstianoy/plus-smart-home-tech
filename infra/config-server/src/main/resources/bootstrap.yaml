spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations:
            - "classpath:/config/commerce/{application}"
            - "classpath:/config/infra/{application}"